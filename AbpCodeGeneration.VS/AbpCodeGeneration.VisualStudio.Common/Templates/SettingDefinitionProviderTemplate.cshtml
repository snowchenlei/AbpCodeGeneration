@inherits RazorEngine.Templating.TemplateBase<CreateFileInput>
using Volo.Abp.Settings;

namespace @Model.Namespace.@Model.DirectoryName
{
    /// <summary>
    /// SettingDefinitionProvider
    /// </summary>
    public class @(Model.LocalName)SettingDefinitionProvider : SettingDefinitionProvider
    {
    /// <summary>
        /// 
        /// </summary>
        /// <param name="context"></param>
        public override void Define(ISettingDefinitionContext context)
        {
            context.Add(
                new SettingDefinition(
                    @(Model.LocalName)Settings.MaxPageSize,
                    "100",
                    isVisibleToClients: true
                )
            );
        }
    }
}
